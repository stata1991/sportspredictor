# Checkpoint: P1.5.1 — Third-Place Ranking Section

**Date:** 2026-05-29

---

## 1. What was done

The 13th entry in API-Football's `/standings` response — `"Ranking of third-placed teams"` with 12 entries — is now rendered as a visually distinct section below the 12-group grid, rather than as an indistinguishable 13th group card.

---

## 2. New files (verbatim)

### `frontend/src/football/utils/partitionStandings.ts`

```typescript
import { StandingEntry } from '../types/standings';

export interface PartitionedStandings {
  realGroups: StandingEntry[][];
  thirdPlaceRanking: StandingEntry[] | null;
}

/**
 * Splits the raw standings array into real groups and the optional
 * third-placed-teams ranking table.
 *
 * Detection: an entry is the ranking table if its group label matches
 * /third/i **or** it contains !== 4 teams (real WC groups always have 4).
 * Either condition routes the entry to the ranking bucket.
 */
export function partitionStandings(
  groups: StandingEntry[][],
): PartitionedStandings {
  const realGroups: StandingEntry[][] = [];
  let thirdPlaceRanking: StandingEntry[] | null = null;

  for (const group of groups) {
    const label = group[0]?.group ?? '';
    const isThirdPlace =
      /third/i.test(label) || group.length !== 4;

    if (isThirdPlace) {
      thirdPlaceRanking = group;
    } else {
      realGroups.push(group);
    }
  }

  return { realGroups, thirdPlaceRanking };
}
```

### `frontend/src/football/components/ThirdPlaceRankingCard.tsx`

```tsx
import React from 'react';
import { Box, Card, Typography } from '@mui/material';
import { StandingEntry } from '../types/standings';
import { flagClass } from '../utils/countryFlag';
import { colors } from '../colors';

const thSx = {
  py: 0.5,
  px: { xs: 0.5, sm: 1 },
  fontWeight: 700,
  fontSize: '0.7rem',
  color: colors.labelText,
  textAlign: 'center' as const,
  whiteSpace: 'nowrap' as const,
};

const tdSx = {
  py: 0.5,
  px: { xs: 0.5, sm: 1 },
  fontSize: '0.8rem',
  color: colors.textSecondary,
  textAlign: 'center' as const,
  borderBottom: '1px solid rgba(255,255,255,0.04)',
};

const TeamCell: React.FC<{ entry: StandingEntry }> = ({ entry }) => {
  const flag = flagClass(entry.team.name);
  return (
    <Box sx={{ display: 'flex', alignItems: 'center', gap: 0.75 }}>
      {flag ? (
        <span className={flag} style={{ fontSize: '1rem', lineHeight: 1 }} />
      ) : entry.team.logo ? (
        <img src={entry.team.logo} alt="" style={{ width: 16, height: 16 }} />
      ) : null}
      <span>{entry.team.name}</span>
    </Box>
  );
};

interface ThirdPlaceRankingCardProps {
  entries: StandingEntry[];
}

const ThirdPlaceRankingCard: React.FC<ThirdPlaceRankingCardProps> = ({
  entries,
}) => (
  <Box data-testid="third-place-section" sx={{ mt: 4 }}>
    <Box sx={{ mb: 2, textAlign: 'center' }}>
      <Typography
        variant="h6"
        sx={{ fontWeight: 700, color: colors.textPrimary }}
      >
        Third-Placed Team Rankings
      </Typography>
      <Typography
        variant="body2"
        sx={{ color: colors.labelText, mt: 0.5 }}
      >
        Best 8 of 12 third-placed teams advance to the Round of 32
      </Typography>
    </Box>

    <Card
      data-testid="third-place-card"
      sx={{
        backgroundColor: 'rgba(26, 26, 26, 0.9)',
        borderTop: `2px dashed ${colors.neutral}`,
        borderLeft: 'none',
        overflow: 'hidden',
      }}
    >
      <Box sx={{ overflowX: 'auto' }}>
        <table style={{ width: '100%', borderCollapse: 'collapse' }}>
          <thead>
            <tr>
              <Box component="th" sx={{ ...thSx, textAlign: 'left', pl: 2 }}>#</Box>
              <Box component="th" sx={{ ...thSx, textAlign: 'left' }}>Team</Box>
              <Box component="th" sx={thSx}>P</Box>
              <Box component="th" sx={{ ...thSx, display: { xs: 'none', sm: 'table-cell' } }}>W</Box>
              <Box component="th" sx={{ ...thSx, display: { xs: 'none', sm: 'table-cell' } }}>D</Box>
              <Box component="th" sx={{ ...thSx, display: { xs: 'none', sm: 'table-cell' } }}>L</Box>
              <Box component="th" sx={{ ...thSx, display: { xs: 'none', md: 'table-cell' } }}>GF</Box>
              <Box component="th" sx={{ ...thSx, display: { xs: 'none', md: 'table-cell' } }}>GA</Box>
              <Box component="th" sx={thSx}>GD</Box>
              <Box component="th" sx={thSx}>Pts</Box>
            </tr>
          </thead>
          <tbody>
            {entries.map((entry) => (
              <tr key={entry.team.id}>
                <Box component="td" sx={{ ...tdSx, textAlign: 'left', pl: 2 }}>
                  {entry.rank}
                </Box>
                <Box component="td" sx={{ ...tdSx, textAlign: 'left' }}>
                  <TeamCell entry={entry} />
                </Box>
                <Box component="td" sx={tdSx}>{entry.all.played}</Box>
                <Box component="td" sx={{ ...tdSx, display: { xs: 'none', sm: 'table-cell' } }}>{entry.all.win}</Box>
                <Box component="td" sx={{ ...tdSx, display: { xs: 'none', sm: 'table-cell' } }}>{entry.all.draw}</Box>
                <Box component="td" sx={{ ...tdSx, display: { xs: 'none', sm: 'table-cell' } }}>{entry.all.lose}</Box>
                <Box component="td" sx={{ ...tdSx, display: { xs: 'none', md: 'table-cell' } }}>{entry.all.goals.for}</Box>
                <Box component="td" sx={{ ...tdSx, display: { xs: 'none', md: 'table-cell' } }}>{entry.all.goals.against}</Box>
                <Box component="td" sx={tdSx}>{entry.goalsDiff}</Box>
                <Box
                  component="td"
                  sx={{
                    ...tdSx,
                    fontWeight: 700,
                    color: colors.homeAccent,
                  }}
                >
                  {entry.points}
                </Box>
              </tr>
            ))}
          </tbody>
        </table>
      </Box>
    </Card>
  </Box>
);

export default ThirdPlaceRankingCard;
```

---

## 3. StandingsPage diff (summary)

- Added imports: `useMemo`, `partitionStandings`, `ThirdPlaceRankingCard`
- `StandingsContent` now calls `partitionStandings(groups)` in a `useMemo`
- Grid renders `realGroups` instead of raw `groups`
- Below the grid: `{thirdPlaceRanking && <ThirdPlaceRankingCard entries={thirdPlaceRanking} />}`
- `LoadingSkeleton` extended with a `data-testid="ranking-skeleton"` Box containing a heading skeleton + 6-row card skeleton

---

## 4. Visual distinction: ThirdPlaceRankingCard vs GroupCard

| Property | GroupCard | ThirdPlaceRankingCard |
|----------|-----------|----------------------|
| Position | 2-col grid | Full-width below grid |
| Left border | 3px solid orange (`#ff6f00`) | None |
| Top border | None | 2px dashed grey (`#90a4ae`) |
| Heading | Group name inside the card | Separate centered heading + subtitle above the card |
| Row count | 4 rows | 12 rows |
| Context | Group name visible in header | Subtitle: "Best 8 of 12 third-placed teams advance to the Round of 32" |

The dashed grey border, full-width layout, centered heading with explanatory subtitle, and 12-row table make it immediately obvious this is not a 13th group card.

---

## 5. Test results

### Partition helper

```
PASS src/football/utils/__tests__/partitionStandings.test.ts
  partitionStandings
    ✓ 13 entries with 13th matching /third/i → 12 real + 1 ranking
    ✓ only 12 real groups (no 13th) → 12 real + null ranking
    ✓ entry whose group matches /third/i but length === 4 → routed to ranking
    ✓ entry with length === 12 but no /third/i match → routed to ranking (length fallback)
    ✓ empty input → empty real groups + null ranking

5 passed
```

### StandingsPage

```
PASS src/pages/football/__tests__/StandingsPage.test.tsx
  StandingsPage
    ✓ shows loading state while fetching
    ✓ loading skeleton includes ranking placeholder
    ✓ shows error state with retry button
    ✓ shows empty state when no groups
    ✓ renders group cards with team names
    ✓ renders multiple group cards
    ✓ displays points and goal difference
    ✓ sets correct page title
    ✓ renders ranking section below grid when ranking exists
    ✓ does not render ranking section when no ranking exists
    ✓ ranking section contains team names from ranking entries

11 passed
```

### Full frontend suite

```
Test Suites: 35 passed, 35 total
Tests:       308 passed, 308 total
```

Was 299 tests across 34 suites (P1.5); now 308 across 35 (+9 new tests, +1 suite).

---

## 6. Deploy

### Commit

```
9a4e5b1 feat: partition standings into group grid + third-place ranking section (P1.5.1)
```

### Frontend (S3 + CloudFront)

```
aws s3 sync build/ s3://fantasyfuel.ai --delete
aws cloudfront create-invalidation --distribution-id E3M2G8G0JUVPK0 --paths "/*"

Invalidation ID: I8Z8830AJTJEP0OACAPP7WDZZR
```

react-snap: 10/10 routes crawled successfully.

---

## 7. Production verification

### Pre-rendered HTML

- Page title: "Group Standings — FIFA World Cup 2026 | FantasyFuel" — PASS
- React root and JS bundle present — PASS
- "Group Standings" heading in pre-rendered HTML — PASS
- HTML size: 22,245 bytes

### After JS hydration (API data loaded)

The API returns 13 groups (confirmed via curl):
- 12 real groups with 4 teams each (Groups A–L)
- 1 ranking entry: "Ranking of third-placed teams" with 12 teams

After hydration:
- 12 group cards render in the 2-column grid, each with orange left border
- Below them: "Third-Placed Team Rankings" section heading (centered, bold)
- Subtitle: "Best 8 of 12 third-placed teams advance to the Round of 32"
- Full-width card with dashed grey top border containing a 12-row table
- Teams listed: South Korea, Qatar, Haiti, Australia, Ivory Coast, Sweden, Iran, Saudi Arabia, Iraq, Austria, Uzbekistan, Ghana
- Same responsive column collapse applies (W/D/L hidden on mobile, GF/GA hidden on tablet)

---

## 8. Surprises

None. The implementation was straightforward. The pre-rendered HTML doesn't contain the dynamic content (group cards, ranking section) because react-snap can't reach the API at build time. This is the same behavior as SchedulePage — the shell pre-renders, JS hydrates and fetches data.

---

## 9. Open questions

None. The partition logic handles all known cases (regex match, length mismatch, both absent). If API-Football changes the label or format, either condition will still catch it.

---

## 10. Acceptance criteria checklist

- [x] `partitionStandings` present and tested (5 cases: 4 specified + empty input)
- [x] Grid renders 12 real groups, no regression
- [x] Third-place section renders below with heading, subtitle, 12-entry table
- [x] Section is visually distinct from group cards (dashed grey border vs solid orange, full-width vs 2-col, separate heading)
- [x] No render when no 13th entry exists (tested)
- [x] Loading skeleton extended (`data-testid="ranking-skeleton"`)
- [x] Full frontend suite green: 35 suites, 308 passed
- [x] Deployed; live verification confirms the visual distinction
